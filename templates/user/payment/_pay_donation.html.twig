{{ form_start(pay_donation, {'action': path('pay_donation', {'id': campaign.id}), 'attr': {'id': 'donation-form'} , 'method': 'POST'}) }}
<div class="form-group row">
    <div class="col-2 col-form-label border-right">{{ form_label(pay_donation.amount) }}</div>
    <div class="col-9">
        {{ form_errors(pay_donation.amount) }}
        {{ form_widget(pay_donation.amount) }}
    </div>
</div>
<div class="form-group row mr-5">
    <div class="ml-auto">{{ form_widget(pay_donation.push) }}</div>
</div>
{{ form_end(pay_donation) }}


